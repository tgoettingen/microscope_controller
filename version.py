"""Central version information for the Microscope Controller application.

Import this module anywhere to access the canonical version string:

    from version import APP_VERSION, APP_NAME
"""

APP_NAME = "Microscope Controller"
APP_VERSION = "1.0.0"

# Human-readable combined string, e.g. "Microscope Controller 1.0.0"
APP_VERSION_STRING = f"{APP_NAME} {APP_VERSION}"
